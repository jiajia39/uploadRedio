
import apmfile from './apmfile.controller';
import service from './service';
import attachments from './attachments.controller'

export const CrudOperations = {
  service,
};

export default {
  apmfile,
  attachments
};
