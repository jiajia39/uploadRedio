import prodsummary from './prodsummary.controller';
import prodkpi from './prodkpi.controller';

import service from './service';

export const CofcoOperations = {
  service,
};

export default { prodsummary, prodkpi };
